package com.example.cargo_transportation.repo;

import com.example.cargo_transportation.entity.RenderFavor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RenderFavorRepository extends JpaRepository<RenderFavor, Long> {
}